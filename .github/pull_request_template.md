> GitHub is currently the public replica of the authoritative CDC GitLab
> repository. Contributors without CDC GitLab access may use this pull request
> as an external proposal. A maintainer will import accepted commits into a
> GitLab branch with attribution and run the authoritative MR/CI process; this
> pull request is not merged independently into GitHub `main`. See
> `CONTRIBUTING.md` and `AGENTS.md`.

## External contribution

Describe the proposed change and, when one exists, link its GitLab issue or
merge request. You do not need CDC CI or deployment permissions.

- Purpose:
- Parity, browser adaptation, or new branch:
- Validation evidence:
- Privacy/security considerations:
- Known gaps:
