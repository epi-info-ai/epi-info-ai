[package]
name = "epi-info-table2x2"
version = "0.15.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
libm = "=0.2.8"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
