export type MapBackgroundSourceV01 = "street" | "blank" | "offline";
export interface MapStudyAreaV01 { west: number; south: number; east: number; north: number; bufferKm: number; }
export interface MapBackgroundPlanV01 { schema: "epi-gis-map-background/0.1"; source: MapBackgroundSourceV01; offlineAssetId?: string; studyArea?: MapStudyAreaV01; }
export class MapBackgroundPlanErrorV01 extends Error { constructor(message: string) { super(message); this.name = "MapBackgroundPlanErrorV01"; } }
function finite(value: number, label: string, minimum: number, maximum: number): void { if (typeof value !== "number" || !Number.isFinite(value) || value < minimum || value > maximum) throw new MapBackgroundPlanErrorV01(`${label} must be finite and between ${minimum} and ${maximum}.`); }
export function validateMapBackgroundPlanV01(plan: MapBackgroundPlanV01): void { if (plan.schema !== "epi-gis-map-background/0.1") throw new MapBackgroundPlanErrorV01("Map background schema is not supported."); if (plan.source !== "street" && plan.source !== "blank" && plan.source !== "offline") throw new MapBackgroundPlanErrorV01("Map background source is not supported."); if (plan.source === "offline" && (typeof plan.offlineAssetId !== "string" || plan.offlineAssetId.trim() === "")) throw new MapBackgroundPlanErrorV01("Offline map backgrounds require a stored asset id."); if (plan.source !== "offline" && plan.offlineAssetId !== undefined) throw new MapBackgroundPlanErrorV01("Offline asset id is only permitted for offline backgrounds."); if (!plan.studyArea) return; finite(plan.studyArea.west, "Study-area west", -180, 180); finite(plan.studyArea.east, "Study-area east", -180, 180); finite(plan.studyArea.south, "Study-area south", -90, 90); finite(plan.studyArea.north, "Study-area north", -90, 90); finite(plan.studyArea.bufferKm, "Study-area buffer", 0, 1000); if (plan.studyArea.west >= plan.studyArea.east || plan.studyArea.south >= plan.studyArea.north) throw new MapBackgroundPlanErrorV01("Study-area bounds must have positive width and height."); }
export function createMapBackgroundPlanV01(input: Omit<MapBackgroundPlanV01, "schema">): MapBackgroundPlanV01 { const plan: MapBackgroundPlanV01 = { schema: "epi-gis-map-background/0.1", ...input }; validateMapBackgroundPlanV01(plan); return plan; }
